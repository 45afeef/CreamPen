<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <EditText android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".Activity.AddTextActivity"
        android:background="@color/white"
        android:hint="Enter your text here"
        android:gravity="center_vertical"
        android:paddingStart="16dp"
        android:textSize="20sp"
        android:id="@+id/et_text"/>

    <Button
        android:id="@+id/btn_saveText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"

        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_margin="@dimen/fab_margin"
        android:text="Save" />

    <ImageButton
        android:id="@+id/btn_color"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_margin="@dimen/fab_margin"
        app:layout_constraintTop_toTopOf="@id/btn_saveText"
        app:layout_constraintBottom_toBottomOf="@id/btn_saveText"
        app:layout_constraintEnd_toStartOf="@id/btn_saveText"
        android:scaleType="fitCenter"
        android:background="@drawable/ic_creampen" />
</androidx.constraintlayout.widget.ConstraintLayout>